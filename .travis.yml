sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t vin31880/docker-react -f Dockerfile.dev .

scripts:
  - docker run -e CI=true vin31880/docker-react npm run test

